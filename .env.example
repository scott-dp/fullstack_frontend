# Local development backend URL.
# In production, the app is expected to call the backend through the same
# origin via Nginx, so this variable can be omitted.
VITE_API_BASE_URL=http://localhost:8080
